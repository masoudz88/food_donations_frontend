export const username = "masoud";
export const password = "zare";

const credentials = {
    username: "masoud",
    password: "zare"
};

export const validateCredential = (username, password) => {
    return true;
}

export default credentials;